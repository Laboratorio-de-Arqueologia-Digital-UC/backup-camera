name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.9
      uses: actions/setup-python@v5
      with:
        python-version: "3.9"
        
    - name: Install uv
      run: pip install uv

    - name: Install dependencies
      run: |
        uv venv
        uv pip install customtkinter wmi blake3 pillow pyinstaller
        uv pip install -e .
        
    - name: Lint with Ruff
      run: |
        uv run ruff check .
        uv run ruff format --check .

    - name: Type Check with Pyright
      run: |
        uv run pyright .

    - name: Run Tests
      run: |
        uv run pytest tests
        
    - name: Build Executable
      run: |
        uv run python build.py
        
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: BackupCamera-EXE
        path: dist/*.exe
